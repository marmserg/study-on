{{ form_start(form) }}
{#    {{ form_widget(form) }}#}

    <div class="form-rows">
        <div class="form-row">
            {{ form_row(form.name) }}
        </div>
        <div class="form-row">
            {{ form_row(form.code) }}
        </div>
        <div class="form-row">
            {{ form_row(form.description) }}
        </div>
    </div>

    <button class="btn">{{ button_label|default('Сохранить') }}</button>
{{ form_end(form) }}
