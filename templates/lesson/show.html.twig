{% extends 'base.html.twig' %}

{% block title %}{{ lesson.name }} / {{ lesson.course.name }} / Study-on{% endblock %}

{% block body %}
    <h1>{{ lesson.name }}</h1>

    <h3><a href="{{ path('app_course_show', {'id': lesson.course.id}) }}">Курс: {{ lesson.course.name }}</a></h3>

    <div class="content">{{ lesson.content }}</div>

    <hr>

    <a class="btn" href="{{ path('app_course_show', {'id': lesson.course.id}) }}">Назад к курсу</a>
    <a class="btn" href="{{ path('app_lesson_edit', {'id': lesson.id}) }}">Редактировать урок</a>
    {{ include('lesson/_delete_form.html.twig') }}

{% endblock %}
